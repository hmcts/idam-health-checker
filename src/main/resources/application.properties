# Profile to run with.
#
# Setting this to 'dev' means that the properties that
# would derive from an Azure vault are loaded from the dev properties file instead
# in order to test locally.
spring.profiles.active=dev,am,idm,ds-user-store, ds-token-store
server.port = 9292

management.endpoints.web.exposure.include=*
management.endpoints.enabled-by-default=true
management.endpoint.env.enabled=false

management.endpoint.health.enabled=true
management.endpoint.health.show-details=ALWAYS

management.endpoints.web.base-path=/admin
management.endpoints.web.path-mapping.health=health

# ForgeRock AM

am.uri = http://localhost:8080/openam
am.client.name = client

# ForgeRock IDM

idm.uri = http://localhost:18080/openidm

# Azure

vault.base.url = https://somewhere.vault.azure.net/
vault.client.id = 42
vault.client.key = 43

# Azure secrets variable names

am.secret.name.password = am-password
am.secret.name.smoke.test.user.username = smoke-test-user-username
am.secret.name.smoke.test.user.password = smoke-test-user-password



ds.token.store.url=ldap://localhost:1636
ds.token.store.userDN=cn=Directory Manager
ds.token.store.passwordName=Pa55word11
ds.token.store.secret.name.password=ds-token-store-password

ds.user.store.url=ldap://localhost:1639
ds.user.store.userDN=cn=Directory Manager
ds.user.store.password=Pa55word11
ds.user.store.secret.name.password=ds-user-store-password
